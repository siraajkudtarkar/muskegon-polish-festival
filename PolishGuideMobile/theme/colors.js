/**
 * Color tokens for the app.
 *
 * HOW TO USE:
 * Import `colors` and reference semantic color names in styles.
 *
 * Example:
 *   import { colors } from "@/theme";
 *
 *   background: colors.background
 *   color: colors.text
 *
 * RULES:
 * - Do NOT hardcode hex values in components.
 * - If a new color is needed, add it here instead of inline.
 *
 */


export const colors = {
    // Brand
    primary: "#B01F1F",
    disabledRed: "#DFB1AA",
    background: "#F2F0E6",
  
    // Neutrals
    white: "#FFFFFF",
    black: "#000000",
    text: "#2E2A2A",
    grey: "#D9D9D9",

  };